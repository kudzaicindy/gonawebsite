{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch site in Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start local server"
    }
  ]
}
